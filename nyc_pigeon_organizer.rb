def nyc_pigeon_organizer(data)
  new_hash = Hash.new
  color_hash = data[:color]
    color_hash.each do |key, value|
      if new_hash.key? (value[i])
        new_hash[value[i]][:color] = "key"
      else
        new_hash[value[i]] = 
end
